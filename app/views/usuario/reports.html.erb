
<ul class="nav nav-tabs" id="myTab" role="tablist">
  <li class="nav-item">
    <a class="nav-link active" id="semanal-tab" data-toggle="tab" href="#semanal" role="tab" aria-controls="semanal" aria-selected="true">Semanal</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="mensual-tab" data-toggle="tab" href="#mensual" role="tab" aria-controls="mensual" aria-selected="false">Mensual</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="anual-tab" data-toggle="tab" href="#anual" role="tab" aria-controls="anual" aria-selected="false">Anual</a>
  </li>
</ul>
<div class="tab-content" id="myTabContent">
<% @stat = Stat.all %>
  <div class="tab-pane fade show active" id="semanal" role="tabpanel" aria-labelledby="semanal-tab">
  <table>
    <thead>
      <tr>
        <th>imagen atraccion</th>
        <th>nombre atraccion</th>
        <th>capacidad atraccion</th>
        <th>nombre supervisor</th>
        <th>estado ticket</th>
        <th>Descargar</th>
      </tr>
    </thead>
    <tbody>
      <% @stat.each do |stat| %>
        <% @atraccion = Atraccion.find(stat.atraccion_id) %>
        <% @user = User.find(stat.user_id) %>
        <% @ticket = Ticket.find(stat.ticket_id) %>
        <% @estados = TicketEstado.where("tickets_id = #{@ticket.id}") %>
        <% @estados.each do |estados| %>
          <% @estado = Estado.find(estados.estados_id) %>
      <tr>
        <%# t = Time.zone.now %>
        <%# if stat.created_at === t - 1.day %>
            <td><img src="<%= @atraccion.imagen_atc %>" alt="<%= @atraccion.nombre_atc %>" height="100" width="150" /></td>
            <td><%= @atraccion.nombre_atc %></td>
            <td><%= @atraccion.capacidad_atc %></td>
            <td><%= @user.name %> <%= @user.last_name %></td>
            <td><%= @estado.descripcion_est %></td>
            <td><%= link_to 'Descargar Archivo' %></td>

      </tr>
        <% end %>
        <%# end %>
      <% end %>
    </tbody>
  </table>
  </div>
  <div class="tab-pane fade" id="mensual" role="tabpanel" aria-labelledby="mensual-tab">
  <table>
    <thead>
      <tr>
        <th>imagen atraccion</th>
        <th>nombre atraccion</th>
        <th>capacidad atraccion</th>
        <th>nombre supervisor</th>
        <th>estado ticket</th>
        <th>Descargar</th>
      </tr>
    </thead>
    <tbody>
      <% @stat.each do |stat| %>
        <% @atraccion = Atraccion.find(stat.atraccion_id) %>
        <% @user = User.find(stat.user_id) %>
        <% @ticket = Ticket.find(stat.ticket_id) %>
        <% @estados = TicketEstado.where("tickets_id = #{@ticket.id}") %>
        <% @estados.each do |estados| %>
          <% @estado = Estado.find(estados.estados_id) %>
      <tr>
        <%# t = Time.zone.now %>
        <%# if stat.created_at === t - 1.day %>
            <td><img src="<%= @atraccion.imagen_atc %>" alt="<%= @atraccion.nombre_atc %>" height="100" width="150" /></td>
            <td><%= @atraccion.nombre_atc %></td>
            <td><%= @atraccion.capacidad_atc %></td>
            <td><%= @user.name %> <%= @user.last_name %></td>
            <td><%= @estado.descripcion_est %></td>
            <td><%= link_to 'Descargar Archivo' %></td>

      </tr>
        <% end %>
        <%# end %>
      <% end %>
    </tbody>
  </table>
  </div>
  <div claSemanalss="tab-pane fade" id="anual" role="tabpanel" aria-labelledby="anual-tab">
  <table>
    <thead>
      <tr>
        <th>imagen atraccion</th>
        <th>nombre atraccion</th>
        <th>capacidad atraccion</th>
        <th>nombre supervisor</th>
        <th>estado ticket</th>
        <th>Descargar</th>
      </tr>
    </thead>
    <tbody>
      <% @stat.each do |stat| %>
        <% @atraccion = Atraccion.find(stat.atraccion_id) %>
        <% @user = User.find(stat.user_id) %>
        <% @ticket = Ticket.find(stat.ticket_id) %>
        <% @estados = TicketEstado.where("tickets_id = #{@ticket.id}") %>
        <% @estados.each do |estados| %>
          <% @estado = Estado.find(estados.estados_id) %>
      <tr>
        <%# t = Time.zone.now %>
        <%# if stat.created_at === t - 1.day %>
            <td><img src="<%= @atraccion.imagen_atc %>" alt="<%= @atraccion.nombre_atc %>" height="100" width="150" /></td>
            <td><%= @atraccion.nombre_atc %></td>
            <td><%= @atraccion.capacidad_atc %></td>
            <td><%= @user.name %> <%= @user.last_name %></td>
            <td><%= @estado.descripcion_est %></td>
            <td><%= link_to 'Descargar Archivo' %></td>

      </tr>
        <% end %>
        <%# end %>
      <% end %>
    </tbody>
  </table>
  </div>

</div>

<script>
  $('#myTab a').on('click', function (e) {
    e.preventDefault()
    $(this).tab('show')
  })
</script>